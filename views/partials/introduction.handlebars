<div id='intro' class=" my-4 offset">
    <h4 class='card-header font-italic text-center'>ABOUT MYSELF</h4>

    <div class="row mb-2 ">

        <div class='col-12 d-flex flex-column my-2'>
            <p class='text-center lead'>I studied accounting in my college and worked in the field for a year, having
                experiences in handling company's annual
                financial data. I like learning new knowledge and have mastered different skills in this few years. Now I am
                focusing on software engineering and hoping to develop my career in this field.</p>
            <button id='edu' class='btn btn-lg btn-danger '>Education</button>
        </div>
        {{#each edu}}
        <div class='col-md-6 eduGp' style='display:none'>
            <div class='card-body d-flex flex-column align-items-start'>
                <h5 class='card-title '>{{course}}{{details}}</h5>
                <h6 class='card-subtitle text-muted mb-2'>{{organ}}</h6>
                <p class='card-text'></p>
            </div>
        </div>
        {{/each}}

        <div class='col-12 d-flex flex-column my-2 '>
            <button id='workExp' class='btn btn-lg btn-danger'>Work Experience</button>
        </div>

        {{#each work}}
        <div class='col-md-6 workExpGp' style='display:none'>
            <div class='card-body d-flex flex-column align-items-start'>
                <h5 class='card-title '>{{title}}</h5>
                <h6 class='card-subtitle text-muted mb-2'>{{company}}</h6>
                <p class='card-text'>{{contents}}</p>
            </div>
        </div>
        {{/each}}

        <div class='col-md-12 d-flex flex-column my-2 '>
            <button id='extra' class='btn btn-lg btn-danger'>Extracurricular activities </button>
        </div>

        {{#each extra}}
        <div class='col-md-6 extraGp' style='display:none'>
            <div class='card-body d-flex flex-column align-items-start'>
                <h5 class='card-title '>{{title}}</h5>
                <h6 class='card-subtitle text-muted mb-2'>{{organ}}</h6>
                <p class='card-text'>{{contents}}</p>
            </div>
        </div>
        {{/each}}
    </div>
</div>